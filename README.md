# React+NextJs = https://bennettfeely.com/clippy/

## note
```
Slider
https://www.youtube.com/watch?v=l4kFO6VQPfA

getStaticProps -> fetch data and build one only first time
                if data change -> page reload not change (only change when re-start server)
                nếu người dùng khác nhau hiển thị dữ liệu khác nhau thì không được dùng 
getServerSideProps -> default get data change from server
getStaticPaths -> dynamic route
                
```